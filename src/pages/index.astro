---
import Base from '../layouts/Base.astro';
import { SITE } from '../site.config';
import { CATEGORIES } from '../data/categories';
import { toolsInCategory, toolPath } from '../data/tools';
---

<Base title={`${SITE.name} — ${SITE.tagline}`} description={`${SITE.tagline}. Free online converters, text tools and encoders that never upload your data.`} canonical="/">
  <section class="hero">
    <h1>Small text tools that respect your data</h1>
    <p class="lead">
      Every tool on {SITE.name} runs entirely in your browser. There is no upload, no account and no waiting — paste,
      adjust, copy.
    </p>
  </section>

  <section id="tools" aria-labelledby="tools-heading">
    <h2 id="tools-heading" class="sr-only">All tools</h2>
    {CATEGORIES.map((cat) => {
      const tools = toolsInCategory(cat.slug);
      return (
        <div class="category">
          <div class="category-head">
            <h2><a href={`/${cat.slug}`}>{cat.name}</a></h2>
            <p class="muted">{cat.blurb}</p>
          </div>
          <div class="grid">
            {tools.map((t) => (
              <a class="card tool-card" href={toolPath(t)}>
                <h3>{t.name}</h3>
                <p>{t.short}</p>
                <span class="cta">Open tool →</span>
              </a>
            ))}
          </div>
        </div>
      );
    })}
  </section>

  <section class="prose why">
    <h2>Why these tools exist</h2>
    <p>
      Reshaping data or text is a two-minute job that somehow eats twenty: open an editor, fiddle with find-and-replace,
      fix the stray trailing comma, repeat. Each tool here does one job well, shows you the result as you type, and
      stays out of your way.
    </p>
    <ul>
      <li><strong>Private by design.</strong> The pages are static files; the conversion is plain JavaScript on your device.</li>
      <li><strong>Fast on any connection.</strong> No frameworks to download, no round-trips to a server.</li>
      <li><strong>Honest about what changed.</strong> Counters tell you how many items went in, came out and were dropped.</li>
    </ul>
  </section>
</Base>

<style>
  .hero { max-width: 72ch; margin-bottom: 2rem; }
  .lead { font-size: 1.15rem; color: var(--text-muted); }
  .category { margin-bottom: 2.25rem; }
  .category-head { margin-bottom: 0.85rem; }
  .category-head h2 { margin: 0 0 0.15rem; font-size: 1.3rem; }
  .category-head h2 a { color: var(--text); }
  .category-head h2 a:hover { color: var(--accent); text-decoration: none; }
  .category-head p { margin: 0; }
  .grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); gap: 1rem; }
  .tool-card { display: flex; flex-direction: column; padding: 1.1rem 1.3rem; color: var(--text); transition: border-color 0.15s, transform 0.15s; }
  .tool-card:hover { text-decoration: none; border-color: var(--accent); transform: translateY(-2px); }
  .tool-card h3 { margin: 0 0 0.35rem; font-size: 1.05rem; }
  .tool-card p { color: var(--text-muted); font-size: 0.9rem; flex: 1; }
  .cta { color: var(--accent); font-weight: 600; font-size: 0.85rem; }
  .why { margin-top: 1rem; }
</style>
